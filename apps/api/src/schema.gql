# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: Int!
  email: String!
  username: String
  role: UserRole!
  createdAt: DateTime!
}

enum UserRole {
  USER
  GLOBAL_ADMIN
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Workspace {
  id: Int!
  name: String!
  owner: User!
}

type WorkspaceMember {
  id: Int!
  user: User!
  workspace: Workspace!
  role: WorkspaceRole!
}

enum WorkspaceRole {
  ADMIN
  MEMBER
}

type Board {
  id: Int!
  title: String!
  workspace: Workspace!
}

type Task {
  id: Int!
  title: String!
  description: String!
  status: TaskStatus!
  order: Int!
  board: Board!
  assignee: User!
}

enum TaskStatus {
  TODO
  DOING
  DONE
  WAITING
}

type Query {
  me: User!
  workspaces: [Workspace!]!
  myWorkspaceMemberships: [WorkspaceMember!]!
  members(workspaceId: Float!): [WorkspaceMember!]!
  allWorkspaces: [Workspace!]!
  allUsers: [User!]!
  boards(workspaceId: Float!): [Board!]!
  tasks(boardId: Float!): [Task!]!
  task(taskId: Float!): Task!
}

type Mutation {
  register(email: String!, password: String!, username: String): User!
  login(email: String!, password: String!): String!
  createWorkspace(name: String!): Workspace!
  inviteUserToWorkspace(workspaceId: Float!, userId: Float!): Boolean!
  inviteToWorkspaceByEmail(workspaceId: Float!, email: String!): Boolean!
  makeAdmin(workspaceId: Float!, userId: Float!): Boolean!
  assignWorkspaceToUser(workspaceId: Float!, userId: Float!): Boolean!
  deleteWorkspace(workspaceId: Float!): Boolean!
  createBoard(title: String!, workspaceId: Float!): Board!
  createTask(title: String!, description: String = "", boardId: Float!): Task!
  updateTaskStatus(taskId: Float!, status: TaskStatus!): Task!
  reorderTasks(taskId: Float!, newOrder: Float!): Boolean!
  moveTask(taskId: Float!, status: TaskStatus!, order: Float!): Task!
  updateTask(taskId: Float!, title: String!, description: String): Task!
  deleteTask(taskId: Float!): Boolean!
}